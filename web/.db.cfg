<?php

/**
 * Used by {@filesource connect.php} providing clear and powerful way
 * how to keep database login data secure away.
 *
 * Expected return value:
 * <pre>
 * array(4) {
 * 	[host] => 'database hostname e.g. localhost'
 * 	[user] => 'user login to the database server e.g. root'
 * 	[password] => 'password of specified user (if no password is required, set to NULL)'
 * 	[name] => 'concrete name of database to be used'
 * }
 * </pre>
 *
 *
 * This is only sample example. You are supposed to customize the contents
 * of this file e.g. change the values of database login data.
 *
 * @return  array
 * @throws E_USER_ERROR
 *
 *
 */

// set the first expression to boolean literal {@code false} if you are familiar with the basic version of this file
// or implement it yourself
true &&
	trigger_error(
		"Application setup error — The database connection is not configured yet. Please fix this following instructions",
		E_USER_ERROR
	);

// default values
return [ // no BC because of developing further (dh is running on PHP >= 5.4)
	'host' =>		'localhost',
	'user' =>		'root',
	'password' =>	NULL,
	// ============
	'name' =>		'diggyshelper', // the database concrete name
];
